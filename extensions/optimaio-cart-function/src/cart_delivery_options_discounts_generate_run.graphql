query CartDeliveryRunInput {
  shop {
    metafield(namespace: "optimaio_cart", key: "campaigns") {
      value
    }
    localTime{
      date
    }
  }
  cart {
    lines {
      id
      quantity
    }
    deliveryGroups {
      id
    }
  }
  discount {
    discountClasses
  }
}
